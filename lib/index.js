const array = require("./structures/Array")
const arr = new array()
/**
 * Creates a new client
 * @class
 */
class utils {
  constructor(opions) {
    /**
     * @param {arr} this.array - e
     */
    this.array = arr;
  }
}

module.exports = utils;